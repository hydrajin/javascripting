const food = {
  type: 'only pizza'
}
//  Use console.log() to print the types property of the food object to the terminal

// Dot notation
console.log(food.type);

// square bracket notation
//console.log(food['type']);
// BOTH return: only pizza